# STATUS: Объезд препятствий при следовании (F12, F13, F14)

sd: SD036
phase: closed
ba: approved
design: skipped
sa: approved

## Скипы
- BA: —
- Design: экраны и визуальные компоненты не меняются; состояние охраны показывается через существующие каналы наблюдения, канал выбирает СА

## Заметки
- **SD036 ЗАКРЫТ (2026-09-18, решение оператора: «давай закрывать sd036»).** Виртуальный бампер, объезд, охрана `obstacle_guard` и инвариант «нет цели — не едем» работают на стенде в сценах, где человек виден камере: проезды вдоль кровати без касаний (минимум 0.07–0.09 м по плоскости лидара), `obstacle` охраны ни разу, `no_target` при пропадании цели, `NO_SCAN` без лидара. Задачи T1–T6 закрыты. T7 не закрыт полностью: протокол `measure.md` написан, сценарии проезжены частично (кровать ×5, коробка ×1), габарит рулеткой не сверен, вердикт — закрыть по решению оператора. Отметки F12–F14 в каталоге [SD001](../SD001/tech.md) этот SD не ставит (чужой документ): нужен отдельный запрос с SD001 текущим. Не коммичено — коммит и релиз за оператором.
- Версии на закрытии: `mentorpi_msgs` 1.4.0 (`ObstacleAvoidanceStatus`), `motion_control` 0.6.0 (объезд в `motion_control`, нода `obstacle_guard`, конфиги YAML), `mentorpi_bringup` 0.11.0 (без `stub_graph`); пакет `mentorpi_stubs` удалён. Сборка `make build` зелёная, `colcon test motion_control` 6 из 6. На стенде (проверено чтением установленных YAML 2026-09-18) стоят итоговые значения: `person_exclusion_radius 0.25`, `margin_max 0.12`, `influence_range 0.25`.
- Итоговые параметры, изменённые по стенду: `person_exclusion_radius` 0.35 → 0.25, `margin_max` 0.20 → 0.12, `influence_range` 0.40 → 0.25; остальные — стартовые I4.2, I5. Обоснования — в таблице `measure.md` и заметках о проездах 3–4 ниже.
- **Хвосты за пределами SD036:**
  1. **Цель ложится на препятствие** (главное). `person_perception` берёт дальность по ближнему кластеру глубины в рамке (SD022 D6.3); коробка или край стены в рамке ближе человека становятся «человеком», робот «доезжает» до них и стоит (проезд 5). Правка восприятия F08 — кандидат в новый SD «удержание цели за препятствием».
  2. **Потеря цели за высоким препятствием и при объезде предмета на линии** (D4.6, принято). Лечится памятью цели по одометрии `/odom_raw`; в симуляции при 5 с и больше стул на линии проходится. Туда же, в новый SD.
  3. Дребезг `ARC` ↔ `ROTATE` почти каждый тик у препятствия вплотную (проезды 3–4) — защёлка разворота отпускается, как только появляется дуга.
  4. Без лидара робот следует без охраны (решение оператора, негативный сценарий №4) — два проезда подряд прошли так незаметно; кандидат на пересмотр. Лидар, подключённый после старта контура, не подхватывается без `t1ctl restart` (свойство F03, SD006).
  5. Габарит не сверен рулеткой; стартовый — огибающая мешей URDF.
  6. `pre-commit run --all-files` падает на clang-format в 4 файлах `mentorpi_voice` (SD035); в рабочей копии они переформатированы не агентом и лежат в индексе вместе с изменениями SD036.
- Бэги стенда и скрипты разбора: `scratch/sd036/bags/sd036_bed_*` (5 проездов), `scratch/sd036/analyze_bag.py`, `clearance_bag.py`, `exclusion_radius.py` (вне git).
- Заведён 2026-09-16 запросом оператора `/develop F12`. Фича из каталога [SD001](../SD001/tech.md): F12 «Охрана столкновений с предметами», этап 1. Номер SD036 — следующий свободный.
- Что есть на входе (из кода и закрытых SD):
  - `/scan` (`LaserScan`, LD19, около 10 Гц, кадр `lidar_frame`, высота 0.18 м, плоскость параллельна полу) — контракт SD006 для F12;
  - глубина `/aurora/points2` (камера на 0.145 м), частота на стенде плавала от 2 до 9 Гц (SD012, SD022);
  - `/perception/persons` и `/perception/nearest_person` с `x`, `y`, `track_id` в базе робота (F08, F09);
  - `/control/motion_restriction` (`stop_request`, `reason`) публикует заглушка `stub_graph` с `stop_request=false`; `control_mux` по `stop_request` обнуляет команду в любом режиме, режим не меняет (SD011);
  - F11 держит дистанцию удержания `standoff` 0.5 м, задним ходом в следовании не едет (SD025); пульт в Manual может ехать назад и поворачивать на месте.
- F13 (агрегатор safety) и F14 (нет человека — не едем) в каталоге отдельными фичами, не закрыты.
- Ответы оператора на вопросы BA (2026-09-16):
  1. объём — F12, F13 и F14 вместе; мало останавливаться, когда препятствие попадает в зону виртуального бампера, нужно объезжать; алгоритм бампера подсмотреть в старых проектах (`scratch/sd036`);
  2. охрана только в автоследовании;
  3. ни один человек не считается препятствием;
  4. препятствия видит только лидар, ограничения плоскости скана документируем;
  5. при отказе датчика робот едет без охраны;
  6. про стоп и зазор — «ответил в п.1».
- Референс `scratch/sd036` (распакован только в scratchpad сессии, в репозиторий не клался): два ROS 1 пакета.
  - `local_planner-master/include/body.hpp` — ближе всего к бамперу: радиус зоны вокруг препятствия растёт со скоростью (`dynamic_inflation`: `Lower_level 0.3` + (`Upper_level 0.65` − `Lower_level`) · скорость, не больше `Upper_level`); точки пути, попавшие в зону, выталкиваются на её границу (`path_bias`, при двух препятствиях — сумма векторов); затем сглаживание (`tightening`) и проверка «стоим на месте» (`check_zero_speed`). Препятствия там — позы роботов соперника, не скан.
  - `local_planner-master/temp/*` — ранние варианты: потенциальное поле (притяжение к цели, отталкивание `0.5·KP·(1/d − 1/R)²` внутри `radius_pot`) по costmap. Код не компилируется, это черновики.
  - `global_planner-master` — Theta*/A* по grid map и симулятор `mover`. Для следования без карты не нужен.
  - Слов «bumper»/«бампер» в коде нет; бампером прочитан механизм `dynamic_inflation` + `path_bias`.
- Прочитано без прямого ответа, проверить на гейте:
  - ограничения SD025 сохранены: задний ход не используется, доворота на месте нет; без доворота объезд невозможен — стоп;
  - оператор видит состояние охраны (свободно, объезд, стоп, нет данных лидара); канал показа выбирает СА;
  - приёмка качественная, как в SD025;
  - `Manual` без охраны — это и есть явное решение по границе F23 из SD001.
- BA черновик 2026-09-16: [`solution.md`](solution.md).
- Правки оператора на гейте BA (2026-09-16):
  1. «Нужно дать бОльшую свободу»: пока камера и глубина видят цель, а лидар видит препятствие, робот старается объехать. В референсе бампер обтекал любые преграды «как вода», но там были омни-колёса. Пункты 2–4 (показ состояния охраны, приёмка, `Manual` без охраны) — «ок».
- Как правка 1 внесена в `solution.md`:
  - на время объезда сняты ограничения SD025: разрешены разворот на месте и отъезд назад; без препятствий в зоне бампера SD025 действует как прежде;
  - стоп только когда любой манёвр (вперёд, разворот, назад) ведёт в предмет;
  - бампер действует при любом манёвре; при выборе манёвра робот по возможности держит цель в кадре;
  - потеря цели во время объезда по-прежнему даёт стоп (F14);
  - отъезд назад — прочтение слов «бОльшую свободу», прямо не назван. Проверить на гейте.
- BA утверждён 2026-09-16 («ок»), вместе с отъездом назад на время объезда. Дизайн пропущен: экранов нет. Этап — системный анализ.
- Ответы оператора на технические вопросы СА (2026-09-16):
  1. алгоритм — перебор манёвров (дуги, разворот на месте, назад) с оценкой, в которой есть отталкивание из референса;
  2. объезд в `motion_control`, независимый стоп — новая нода `obstacle_guard` вместо `stub_graph`;
  3. наблюдаемость — только топик статуса и `reason` в `/control/motion_restriction`, без `t1ctl` и маркеров Foxglove;
  4. до стенда — юнит-тесты и 2D-симуляция замкнутого контура в `colcon test`.
- СА утверждён 2026-09-16 в UI режима Plan. Канон: [`tech.md`](tech.md), 39 пунктов, задачи T1–T7. Решения, которых не было в вопросах: правило выхода (кандидат допустим, если уводит от уже близкого предмета) в планировщике и охране; F14 держится дважды — гейтом `motion_control` и стопом `no_target` в `obstacle_guard`; общий `footprint.yaml` для обеих нод; параметры `motion_control` переезжают из launch в YAML; пакет `mentorpi_stubs` удаляется; габарит взят с мешей URDF (0.341 × 0.246 м), сверка рулеткой на T7.
- Оператор дал команду на T1 2026-09-16 («ок»).
- **T1 реализован 2026-09-16, ждёт QA оператора.** `obstacle_points.hpp` (скан в базу через позу лидара, фильтр самоотражений, исключение людей), `virtual_bumper.hpp` (зазор до габарита, точная дуга, прокат с правилом выхода, запас по скорости), тесты `test_obstacle_points` и `test_virtual_bumper`, строки в `CMakeLists.txt`.
  - Проверено агентом на хосте (`g++ -std=c++17 -Wall -Wextra -Wpedantic`, без ROS): три теста `ok`, включая прежний `test_follow_control`; предупреждений нет; clang-format 21.1.8 из хука на файлах T1 чистый. `colcon build/test` в сборщике агент не запускал (AGENTS.md: сборку запускает оператор).
  - `pre-commit run --all-files` падает на clang-format из-за файлов `mentorpi_voice` из SD035 (хук их переформатирует). К SD036 это не относится; правки хука в этих файлах откачены, чужой код не тронут.
- T1 принят оператором 2026-09-17 («ок»). Оператор разрешил агенту самому запускать сборку и тесты в сборщике (деплой и движение робота — по-прежнему оператор). T1 в сборщике arm64: `colcon build` зелёный, `colcon test` 3 из 3.
- **T2 реализован 2026-09-17, ждёт QA оператора.** `avoidance_planner.hpp` (`plan_avoidance`, `reset_avoidance`), тест `test_avoidance_planner` (AC1–AC7, 1000 случайных сцен по 5 тиков). Сборщик arm64: сборка без предупреждений, `colcon test` 4 из 4. `follow_control.hpp` не менялся.
  - Деталь реализации в рамках D4.5: кандидат-дуга оценивается уже после рампы и страгивания, поэтому прокат проверяет ровно ту команду, что уходит на выход.
  - Время тика на хосте x86 в худшем случае (450 точек полукольцом, перебраны все дуги и разворот): 1.3 мс.
  - **Наблюдение, вынесено оператору:** на скорости следования рампа торможения D4.5 (`decel_linear 0.6` → минус 0.03 м/с за тик) оставляет дугам скорость 0.22–0.25 м/с и доворот не больше ~0.2 рад/с. Такие дуги не обходят предмет по курсу, лестница уходит в `ROTATE` — робот встаёт и разворачивается на месте, а не обтекает. Замер: предмет в 0.45 м по курсу, с места — `ARC` (`v 0.10, ω −1.0`), на ходу 0.25 м/с — `ROTATE`. Предложение: на время объезда замедление дуги мгновенное, разгон — по рампе (правка D4.5). Решение за оператором до T4.
- `src/mentorpi_voice` (4 файла SD035) снова переформатированы clang-format после 2026-09-17 не агентом (вероятно, прогон `pre-commit` при проверке T1). Агент их не трогал и не откатывал.
- T2 принят оператором 2026-09-17 («ок»), вместе с предложением по D4.5. **D4.5 изменён в `tech.md`:** дуга на время объезда замедляется до скорости кандидата сразу, разгон — по рампе `accel_linear` со страгиванием. После правки предмет в 0.45 м по курсу на ходу 0.25 м/с даёт `ARC` (`v 0.10, ω −1.0`), а не `ROTATE`; в `test_avoidance_planner` добавлена проверка этого и разгона после дуги.
- **T3 реализован 2026-09-17, ждёт QA оператора.** `guard_decision.hpp` (`decide_guard`, константы `reason`), тест `test_guard_decision` (AC1–AC6; AC6 — команды планировщика на сценах T2 и 1000 случайных сценах по 5 тиков ни разу не дали `obstacle`). Стартовые значения тестов вынесены в общий `test/sd036_fixtures.hpp` (I4.1, I4.2, I4.3, I5), тест T2 переведён на него. Сборщик arm64: сборка без предупреждений, `colcon test` 5 из 5.
  - «Зазор убывает» в D6.1 реализован как `min_clearance < start_clearance − 1 мм`: это то же правило выхода, что у планировщика, но без требования «к концу растёт» — охрана не мешает ехать вдоль предмета, если к нему не приближаешься.
- T3 принят в составе команды оператора 2026-09-17: «делай вплоть до t7, я в сумме проверю» — T4–T6 и подготовка T7 без пауз на QA, проверка оператором суммарно.
- **T4 реализован частично 2026-09-17, не закрыт: найдена дыра в требованиях.** `test/sim_world.hpp` (мир из окружностей и отрезков, лучевой лидар 450 лучей в `lidar_frame` с позой URDF, точная дуга шасси, восприятие по сектору камеры, цепочка `plan_avoidance` → `decide_guard` → `gate_mux`, сброс памяти планировщика по `stop_request`) и `test_avoidance_sim` (10 сцен).
  - Проходят: проём 0.6 м со смещением, стена вдоль пути, тупик (`REVERSE`), окружён (`BLOCKED`, робот не сдвинулся), чужой человек (точки исключены, объезда нет), нет цели и предмет (стоит, `no_target`), цель ушла из кадра во время объезда (ноль на том же тике), нет скана (закон SD025, `no_scan`). Касаний нет ни в одной сцене, `obstacle` охраны на сценах доезда не срабатывает.
  - **Не проходит критерий доезда (AC2 «стул», AC3 «доезд после разворота»):** предмет стоит на прямой робот—человек. Чтобы обойти стул r 0.2 м в 1.2 м перед роботом, нужен уход курса около 35–40°, а полуугол камеры 37° (SD012). Человек выходит из кадра, гейт закрывается, робот встаёт (BA, негативный сценарий №2) и больше не трогается, потому что цель стоит на месте за пределами кадра. Трасса: робот ползёт к стулу прямыми дугами на 0.10 м/с, перебирает сторону (ω −0.25, −0.35, +0.43, −0.5, +0.5), сворачивает поздно, на `yaw 0.63` цель пропадает.
  - Что проверено без успеха (вне репозитория, копия заголовка): перебор `horizon_s` {1.5, 2.5, 3.0} × `kp_repulse` {0.01, 0.05, 0.2} × `influence_range` {0.4, 0.7} × `w_heading` {0.3, 1.0} × `margin_max` {0.20, 0.30} — стул на линии и предмет вплотную теряют цель во всех 72 вариантах; защёлка стороны дуги; штраф поля зрения по всей траектории, а не только в конце; жёсткий запрет манёвров, уводящих цель из кадра (цель не теряется, но робот и не доезжает, крутится перед стулом). Стул, смещённый на 0.15 м от линии, проходит только при `horizon_s 3.0`.
  - В тесте для двух сцен сейчас проверяется безопасность (касаний нет, охрана молчит, `ROTATE` → `ARC` есть), а доезд печатается строкой `OPEN …` и тест не валит. Стартовые значения не менялись: без решения по дыре подбор бессмыслен. **Решение за оператором**, варианты вынесены в чат.
  - Эксперимент с памятью цели (вне репозитория, копия `sim_world.hpp`): если цель пропала из кадра во время объезда, контур ведёт к её последней позиции через одометрию до `N` с. При `N` 1–3 с стул на линии всё равно не проходится (робот ползёт вдоль стула на 0.10 м/с и не успевает); при `N` ≥ 5 с доезд есть у стула на линии (14 с), большого стула r 0.25 (16 с) и дальнего стула (20 с), касаний и `obstacle` охраны нет. Не проходят и с памятью: щит шириной 0.8 м в 1 м по курсу и предмет вплотную шириной 0.5 м — там нужен разворот больше 60°.
- **T5 и T6 реализованы 2026-09-17, стендовая часть ждёт оператора.**
  - `mentorpi_msgs` 1.4.0: `ObstacleAvoidanceStatus.msg` (I1.1).
  - `motion_control` 0.6.0: нода `motion_control` — подписки `/scan`, `/perception/persons`, `/control/motion_restriction`, TF лидара, `NO_SCAN` с законом SD025, `DISABLED` при `enable_avoidance: false`, статус `/pnc/obstacle_avoidance/status` на каждом тике (зазор и точки считаются и вне AutoFollow — для проверки в Manual), сброс памяти по `stop_request` (D5.4), валидация D5.3; новая нода `obstacle_guard` (D6); общий ROS-код входов `include/motion_control/ros_inputs.hpp` (скан → точки через TF, список людей) — одна реализация для обеих нод; конфиги `config/footprint.yaml`, `motion_control.yaml`, `obstacle_guard.yaml` (значения SD025 перенесены без изменений, новые — стартовые I4.2, I5).
  - `mentorpi_bringup` 0.11.0: `stage1.launch.py` без `stub_graph` и аргумента `rate_hz`, `motion_control` и `obstacle_guard` из YAML; `exec_depend mentorpi_stubs` убран. Пакет `src/mentorpi_stubs` удалён. README: пакеты и топики.
  - Проверки агента: `make build` зелёный (15 пакетов, без предупреждений, 9 мин под эмуляцией); `colcon test motion_control` 6 из 6; clang-format 21.1.8 и `git diff --check` чистые на всех файлах SD036; flake8/black/isort по `stage1.launch.py` чистые. Дымовой запуск обеих нод в контейнере сборщика без сети: старт с параметрами из YAML, строки старта печатают все значения, `obstacle_guard` публикует решение (`stop_request false reason=''` без входов), `margin_min:=0.5` роняет `motion_control` с ошибкой `margin_min <= margin_max <= influence_range is required`. Изменений в `mentorpi_control`, `mentorpi_platform`, `host/t1ctl`, `follow_control.hpp` нет.
  - Попутно: дымовой прогон с неверным параметром оставил в корне репозитория core-дамп qemu (107 МБ) — удалён агентом.
  - Не сделано агентом: AC на живом графе (T5 AC3–AC6, T6 AC1–AC6) — это стенд, собраны в таблицу «Граф» в `measure.md`. Задеплоенного на Pi нет: `make deploy` за оператором. В `stage1.launch.py` нет аргумента `enable_lidar` у systemd-запуска, поэтому «нет скана» на стенде проверяется остановкой драйвера (`pkill -f ldlidar_stl_ros2_node`), а не перезапуском с аргументом.
- **T7 подготовлен 2026-09-17:** [`measure.md`](measure.md) — безопасность, подготовка, таблица проверок графа G1–G12 (T5, T6), сверка габарита, команда бэга, сценарии S1–S9 по BA (S9 — предмет на линии к человеку, наблюдение находки T4), наблюдения D4.6, таблица итоговых параметров, вердикт. AC2–AC5 T7 — проезды оператора.
- Хвост `src/mentorpi_voice`: 4 файла SD035 по-прежнему в рабочей копии переформатированы clang-format не агентом; агент их не трогал.
- **Решение оператора по находке T4 (2026-09-18): вариант 1 — принять ограничение.** «В памяти хранить особо нечего, потому что ещё нет стека навигации»: память цели отложена до F16–F19. Правки: D4.6 в `tech.md` дополнен ограничением обзора камеры; AC2 и AC3 T4 уточнены (доезд через предмет ровно на линии не требуется, для него проверяется ограничение); в `solution.md` к НФТ про камеру добавлено одно предложение про предмет на линии и вплотную — это уточнение уже утверждённого требования «разворот может увести человека из кадра», новых требований нет; в `measure.md` поправлены ожидания S3 и S9. Стартовые значения параметров не менялись: `horizon_s 2.0` сцены не ломал, но доезд с предметом вплотную давал только в одной подобранной расстановке.
- **T4 закрыт 2026-09-18.** `test_avoidance_sim`: стул, перекрывающий линию краем (смещение 0.25 м), — доезд за ~13 с; стул ровно на линии и предмет вплотную — касаний нет, охрана молчит, цель выходит из кадра, дальше робот стоит (проверка `expect_limitation`). Строк `OPEN` больше нет.
- **Стенд, проезд 1 (2026-09-18, оператор): робот врезался правым бортом в кровать.** Бэг `sd036_bed_20260918_123641` (копия в `scratch/sd036/bags/`, разбор `scratch/sd036/analyze_bag.py`).
  - Причина — лидар не работал: в бэге 0 сообщений `/scan`; весь участок следования (8.2–25.1 с) статус объезда `NO_SCAN`, охрана `stop_request false, reason no_scan`, точек препятствий 0. Контур повёл себя по BA (негативный сценарий №4, решение оператора «ехать без охраны»): следование без объезда. Объезд и охрана в этом проезде не работали вовсе, поэтому о них проезд ничего не говорит.
  - Стенд после проезда (только чтение): `t1ctl status` — `lidar degraded`; на хосте Pi нет ни `/dev/ldlidar`, ни `/dev/lidar`; процесс драйвера лидара не запущен, работает только `scan_to_scan_filter_chain`; у `/scan` 0 издателей. Лидар не определился по USB — железо или кабель, код SD036 ни при чём.
  - Попутно в бэге: темп записи неровный (статус ~19 Гц на участке следования, в среднем за 20 мин записи ~0.9 Гц) — запись шла 20 минут, после проезда сообщений почти нет; для разбора хватает.
- **Стенд, проезд 2 (2026-09-18, бэг `sd036_bed_20260918_131049`): лидара снова нет.** `/scan` — 0 сообщений; весь AutoFollow (17.3–98.4 с) статус `NO_SCAN`, охрана `no_scan`, точек 0. Объезд опять не проверен.
  - Стенд (только чтение, 21:13 по часам Pi): `/dev/ldlidar → ttyUSB1` появился в 21:03 — лидар переподключили, — а контур поднят в 20:30 и после этого не перезапускался. `lidar_layer` выбирает порт один раз при старте launch: в 20:30 устройства не было, драйвер не поднялся; процесса драйвера нет, `/scan_raw` без издателя, работает только `scan_to_scan_filter_chain`. `t1ctl status` — `lidar degraded`. Нужен `t1ctl restart` с подключённым лидаром; агент его не делал: после перезапуска режим по умолчанию AutoFollow, робот может поехать.
  - Горячее подключение лидара без перезапуска контура не подхватывается — свойство F03 (SD006), вне SD036.
- **Стенд, проезд 3 (2026-09-18, бэг `sd036_bed_20260918_133606`): лидар работает, объезд активен.** `/scan` ~10 Гц (791 сообщение). Статусы: `FREE` 514, `AVOIDING` 336, `INACTIVE` 772 тика; `NO_SCAN` и `BLOCKED` нет. Манёвры `ARC`, `ROTATE`, `REVERSE` встречаются. Охрана `obstacle` ни разу; `no_target` при пропадании человека. Разбор: `scratch/sd036/clearance_bag.py`, `scratch/sd036/exclusion_radius.py`.
  - Минимальный зазор габарита до точек лидара за проезд — 0.071 м (48–52 с, правый борт у кровати); по плоскости скана касания не было. Было ли касание физически — вопрос оператору.
  - **Находка: круг исключения человека прячет кровать.** Когда человек стоит вплотную к кровати, край кровати в скане — прямая линия, проходящая через центр человека (точки на расстоянии 0.04–0.33 м от центра). При `person_exclusion_radius 0.35` планировщик видел зазор 0.77 м вместо реальных 0.11 м (18–27 с), 0.13 вместо 0.08 (44–48 с), и на 41–43.5 с ехал прямо к кровати дугой `ω = 0` на 0.10–0.15 м/с, потому что видел 0.12–0.14 м вместо 0.09–0.10 м — выше запаса `margin(0.10) = 0.11`. Перебор радиуса по бэгу: при 0.25 м и меньше планировщик видит кровать в полный рост во всех трёх окнах (0.108, 0.098, 0.083 м), а ноги идущего человека (53–56 с, точки ног в 0.12–0.22 м от центра) при 0.25 м ещё исключаются; при 0.20 м часть точек ног остаётся препятствием.
  - Попутно: на 28–30 с манёвр переключается `ARC` ↔ `ROTATE` почти каждый тик (защёлка разворота отпускается, как только появляется дуга, и дуга тут же пропадает) — дребезг, на стенде виден как подёргивание. Кандидат на правку после приёмки.
- **`person_exclusion_radius` 0.35 → 0.25 (2026-09-18, по согласию оператора «продолжай»).** Правка в `config/footprint.yaml` (общий для обеих нод), в значениях по умолчанию `motion_control` и `obstacle_guard`, в фикстурах тестов и симуляции; `tech.md` I4.3 и таблица параметров `measure.md` обновлены. Версия `motion_control` не поднималась: 0.6.0 ещё не принята, правка — подбор стартового значения внутри T7.
- **Стенд, проезд 4 (2026-09-18, бэг `sd036_bed_20260918_151838`, радиус 0.25): жалоба оператора — «боится кровать, сильно замедляется, слишком далеко, не огибал угол, а остановился».** Касаний нет; минимальный зазор 0.087 м на старте, 0.147 м в конце.
  - «Остановился у угла»: в конце человек в 0.53 м от центра робота (стоп 0.5 + коридор 0.05) — цель достигнута по закону SD025, огибать было незачем.
  - «Далеко от кровати»: на 12–17 с робот ехал `AVOIDING/ARC` прямо вдоль кровати в 0.22–0.25 м на 0.25 м/с — это `margin_max 0.20` плюс `free_hysteresis 0.03`; доворот к человеку (он справа, за кроватью) вводил кровать в бампер.
  - «Замедляется»: у человека — штатное торможение закона SD025 (`kp_lin` × (дальность − 0.5)); вдоль кровати дополнительно тормозило отталкивание (`influence_range 0.40`): у медленной дуги меньше `J_obs`.
  - Сцена «вдоль стены к человеку за углом» в симуляции теряет цель при любых значениях — это ограничение обзора D4.6, не бампер.
  - На старте (8.6–9.3 с) снова дребезг `ARC` ↔ `ROTATE` почти каждый тик.
- **Подбор по проезду 4 (2026-09-18):** `margin_max` 0.20 → 0.12, `influence_range` 0.40 → 0.25 (YAML, значения по умолчанию ноды, фикстуры тестов). В `test_avoidance_planner` предмет сцен AC2/AC6 передвинут с 0.45 на 0.41 м: при новых запасах номинал с места на 0.45 м проходит бампер, объезд не включается; окно, где тест осмыслен, — 0.40–0.42 м.
- **Стенд, проезд 5 (2026-09-18, бэг `sd036_bed_20260918_181748`, оператор): коробка 55 см между роботом и человеком — робот видел человека и остановился перед коробкой, не объезжал.** Плюс со слов оператора (без бэга): «наполовину за стеной — встаёт перед ней»; «подбирается близко к препятствию и теряет человека, препятствие закрывает обзор».
  - Бэг не забран: Pi не отвечает ни по 192.168.88.56, ни по 192.168.149.1, ни по 192.168.2.2 (машина агента в 192.168.88.0/24).
  - Версия по коду, до проверки бэгом: дальность человека `person_perception` берёт по ближнему кластеру облака внутри рамки детекции (`nearest_cluster_in_bbox`, слой 0.35 м, ≥ 40 точек, SD022 D6.3). Коробка или край стены внутри рамки ближе человека → «человек» оказывается на их поверхности → робот считает, что доехал до дистанции удержания, и стоит; круг исключения 0.25 м вокруг такой «цели» заодно прячет коробку от лидара. Это восприятие F08, не объезд; `tech.md` D9 фиксирует контракт `/perception/*` неизменным — правка вне объёма SD036.
  - Потеря человека за высоким препятствием вблизи: камера на 0.145 м, коробка 55 см закрывает человека, цель теряется, робот встаёт (негативный сценарий BA №2). Лечится только памятью цели (отложена оператором 2026-09-18); для неё нужна лишь одометрия `/odom_raw`, стек навигации не требуется.
  - **Бэг забран (Pi снова в сети) — версия подтверждена.** На 28–37 с «человек» в `/perception/persons` подъезжает с (0.97, −0.05) до (0.54, −0.02) м, а ближняя грань коробки по лидару всё время на 0.05–0.10 м ближе: (0.86, +0.06) → (0.49, +0.13). То есть цель легла на коробку (ближний кластер глубины в рамке — её грань), дальность цели 0.54 м → коридор удержания → статус `FREE`, команда ноль: робот «доехал». Объезда не было, потому что ехать было некуда; `AVOIDING` за проезд 9 тиков. Круг исключения вокруг такой «цели» при этом снимал 41–45 точек коробки. То же на 40–50 с: «цель» (0.45, +0.11), грань коробки (0.35, +0.23).
