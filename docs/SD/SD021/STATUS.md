# STATUS: Инверсия оси X цели перцепшна (кадр камеры)

sd: SD021
phase: qa-handoff
ba: approved
design: skipped
sa: approved

## Скипы
- BA: —
- Design: экраны/визуальные компоненты не меняются — фикс кадра камеры (TF/экстринсики), UI нет.

## Заметки
- **SD021 ЗАКРЫТ.** Цель SD — инверсия оси X: закрыта сбросом `depth_cam.rpy→(0,0,0)` на стенде (T2 D2.2). Код/URDF/F11 не менялись. T3 бэг `f11_follow_20260905_080816`: все valid `x>0`, пеленг ~19° вместо ±165°.
- Ось Y после фикса X осталась инвертированной (слева от камеры `person.y<0` → F11 крутит вправо). В коде знак не инвертируем; yaml после T2 не трогаем. Не в объёме закрытия SD021; рекуррентность `yaw=π` в `corner` — SD022.
- design-review skipped: UI не менялся.
- Заведён из QA SD020/F11: перцепшн отдаёт цель как x<0 (позади) при человеке спереди.
- solution.md заполнен полностью (Проблема/Решение/Сценарии/ФТ/НФТ).
- Объём: фикс кадра + сквозная переприёмка F11. Диагностика корневой причины (URDF F07 / калибровка SD012 / конвенция драйвера) — внутри SD021. Постоянный автотест не вводим. Приёмка на F11 как представителе /perception/*.
- Доказательства: бэг pi@192.168.2.2:/home/ubuntu/mentorpi_t1_ws/bags/f11_follow_20260904_182253, видео /Users/roman/Downloads/f11_run1.mp4.
- tech.md утверждён (Plan): 9 пунктов (D1.1–D4, I1–I2), задачи T1 диагностика → T2 фикс → T3 переприёмка F11. Диагностика-первый: без диагноза фикс не начинаем.
- T1 сделан: виновник **(b)** — `depth_cam.yaw=π` в yaml (corner), optical factory. Разворот `Rz(π)+pitch`, не `Ry(π)`; знак Y в TF тоже инвертирован.
- T2 сделан (D2.2): на Pi сброшен `depth_cam.rpy→(0,0,0)`, высота 0.145 м сохранена. Код/URDF не менялись.
- T3 QA: X в порядке (`x>0` спереди), Y инвертирована (слева от камеры `person.y<0`). Знак Y в коде не инвертируем.
- Рекуррентность вынесена в отдельный SD: источник записи `yaw=π` (стадия `corner` калибровки) диагностирован на уровне кода и передан в [SD022](../SD022/solution.md) (упрочнение солвера + гейта записи, A+B). Объём SD021 остаётся T1–T3 (фикс кадра + переприёмка F11), сюда не расширяется.
