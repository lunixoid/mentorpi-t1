# STATUS: Подавление шума по двум микрофонам

sd: SD035
phase: closed
ba: approved
design: skipped
sa: approved

## Скипы
- BA: —
- Design: экраны и визуальные компоненты не меняются, поведение для человека остаётся как в SD031 и SD033

## Заметки
- **SD035 ЗАКРЫТ (2026-09-16, решение оператора: «вариант 1»).** Двухканальная ступень выигрыша не дала, программный путь признан исчерпанным; дальше оператор меняет микрофон. Задачи T1–T4 закрыты, T5 (ступень в ноде) и T6 (приёмка на ходу) не делались. Канон: [`tech.md`](tech.md). Не коммичено, коммит и релиз за оператором.
- Что осталось в коде: `fft.hpp`, `dual_channel.hpp` (`none`, `nlms`, `coherence`), ступень в `CommandPipeline` перед сведением в моно и флаги `--dual`, `--nlms-*`, `--coherence-*` в `voice_bench`. В ноде ступени нет: `voice_command` параметр `dual` не читает, конвейер получает значение по умолчанию `none`. На роботе поведение ровно как после SD033.
- Итог замеров: на ходу с 3–4 м обычным голосом 0 из 6 во всех шести вариантах; `nlms` обнулил и контрольную сессию стоя (речь попадает в опорный канал и вычитается вместе с шумом); `coherence` ближний случай не испортил (стоя 6 из 6), но команд на ходу не добавил; лучшей осталась одноканальная цепочка SD033 (`speexdsp`, 9 из 18). Ложных команд нет ни у одного варианта. Ступень стоит 8–11 % времени обработки.
- Для выбора железа (следующий шаг оператора): отношение сигнал/шум на ходу с 3–4 м в полосе речи от −7 до −1 дБ (SD033); каналы WonderEcho почти не коррелированы (0,30–0,42 на ходу, 0,58 стоя), направленности у него нет. Нужен микрофон с направленностью или вынос его от гусениц.
- Версии на закрытии: `mentorpi_voice` 0.3.0 (было 0.2.0): добавлены модули и ступень, по умолчанию выключена. Остальные пакеты не менялись. Образ сборщика, runtime-образ и `mk/provision.sh` не менялись, выкладка не нужна.
- Хвосты: записи и разметка SD035 в `scratch/sd035/` вне git; скрипт стенда `scratch/sd033/t1_stand.sh` умеет сессии `d*` и пригодится, если понадобятся новые записи; на стенде стоит сборка SD033, код SD035 на робота не выкладывался.
- Заведён 2026-09-16 по решению оператора во время SD033 («заведи SD под вариант 2»). Заготовка: требований нет, вопросы BA не задавались. Работа начинается отдельным запросом.
- Зачем: в SD033 голос на ходу проходит только примерно с 1 м обычным голосом или с 3–4 м громко. Чтобы вернуть дальность 3–4 м обычным голосом, нужен другой способ подавления шума, а не тот, что уже проверен.
- Что известно из замеров SD033 T4 (2026-09-16), записи и бенч остались в `scratch/sd033/` вне git:
  - WonderEcho Pro отдаёт захват в два канала, и это два разных микрофона: одинаковых отсчётов 0,1–0,2 %, разность каналов всего на 2–4 дБ ниже самого канала, лучшая взаимная корреляция 0,35 при сдвиге до 0,5 мс. Шум между каналами почти не коррелирован;
  - усреднение каналов, которое конвейер SD033 делает сейчас, даёт не больше 3 дБ;
  - отношение сигнал/шум на ходу при 3–4 м обычным голосом от −7 до −1 дБ в полосе речи (речь около −40 dBFS, шум гусениц около −39…−33 dBFS);
  - одноканальные очистки на этом материале: `speexdsp` вытягивает команду на границе (1 м: 0 → 2 из 3), `rnnoise` пережимает речь и мешает, без очистки с 3–4 м обычным голосом 0 из 3;
  - `partial_stable_ms`, `min_confidence` и сила подавления SpeexDSP на результат не влияют.
- Что можно попробовать в этом SD (гипотезы, не требования): адаптивный подавитель (второй канал как опора для шума, например NLMS), двухканальные спектральные методы, оценка выигрыша по тем же записям бенчем `voice_bench` из SD033.
- Инструменты, которые уже есть после SD033: запись звука в `voice_command` (`record_dir`, два канала), офлайн-бенч `voice_bench` с разметкой `labels.tsv`, конвейер `CommandPipeline` с подключаемой очисткой (`Denoiser`).
- Ограничение SD033, которое здесь придётся пересмотреть: «решение только программное, микрофон и динамик остаются на прежнем месте».
- Работа начата 2026-09-16 («приступаем к sd035»). Ответы оператора на вопросы BA:
  1. цель: «заметно лучше чем сейчас», числового порога нет;
  2. железо в SD035 не трогаем; если не получится, оператор сменит микрофон, и это уже вне этого SD;
  3. набор команд, контракт именованной команды и ответ в динамик прежние (SD031, SD033);
  4. приоритет ошибок прежний: пропущенная команда хуже ложной;
  5. приёмка — проезд оператора с вердиктом.
- Без прямого ответа остался предел нагрузки на CPU. Прочитано как в SD033: порога нет, принимается то, что получится. Проверить на гейте.
- BA черновик 2026-09-16: [`solution.md`](solution.md). Утверждён 2026-09-16 («ок»); место про предел нагрузки оператор не поправил. Дизайн пропущен: экранов нет. Этап — системный анализ.
- Ответы оператора на технические вопросы СА (2026-09-16):
  1. сравнить на записях два метода (адаптивный подавитель по второму каналу и маску по когерентности каналов), выбрать по цифрам;
  2. снять новую серию записей в два канала, старых `s5`–`s7` (по три команды) для вердикта мало;
  3. прогон приёмки: 3–6 команд, а не 10;
  4. добавленная задержка ответа 0,2–0,3 с приемлема.
- **T2 закрыт 2026-09-16** (всё в сборщике): `fft.hpp` (радикс-2 на месте, окно Ханна), `dual_channel.hpp` (`none`, `nlms`, `coherence`), тесты `test_fft` и `test_dual_channel`. `pre-commit` чистый, сборка без предупреждений, `colcon test` 11 из 11.
  - AC1: прямое и обратное преобразование сходятся, косинус даёт пик в своей полосе, размер 300 и 1 бросают исключение, окно Ханна при перекрытии 50 % складывается в единицу;
  - AC2: `none` побитно равен полусумме каналов (то же, что `mix_to_mono`);
  - AC3: `nlms` на синтетике (опора — тот же шум, в основном канале он задержан и отфильтрован) подавляет шум на 76,3 дБ. Цифра идеальная именно потому, что опора идеальная: на записях с двух близких микрофонов так не будет, речь тоже попадает в опорный канал;
  - AC4: `coherence` (порог −18 дБ) держит общий тон в пределах 0,1 дБ и давит некоррелированную часть на 13,6 дБ;
  - AC5 и AC6: на нулях и полной шкале переполнения нет; неизвестное имя, один канал, `nlms_taps: 4`, `coherence_fft: 300`, `coherence_floor_db: +5` дают `none` и текст ошибки.
- T1: сессии SD035 (`d1_drive`, `d2_drive_noise`, `d3_drive_talk`, `d4_still`) добавлены в скрипт стенда, пишутся в два канала и складываются в `scratch/sd035/rec/`.
- T1 записи сняты оператором 2026-09-16, все в два канала, отсечения нет: `d1_drive` 30,3 с (пик −24,3 dBFS), `d2_drive_noise` 25,8 с (−23,4), `d3_drive_talk` 23,8 с (−24,8), `d4_still` 25,6 с (−33,3). Разметка `scratch/sd035/rec/labels.tsv`: по шесть команд в `d1_drive`, `d2_drive_noise`, `d4_still`, пусто в `d3_drive_talk` (AC2).
  - AC3, различие каналов в новых записях: одинаковых отсчётов 0,3–4,3 %; разность L−R на ходу всего на 2,6–2,9 дБ ниже самого канала, стоя на 3,2 дБ ниже; лучшая взаимная корреляция на ходу 0,30–0,42 при сдвиге около −10 отсчётов (0,2 мс), стоя 0,58 при сдвиге +8. То есть на ходу шум между микрофонами коррелирован ещё слабее, чем в замерах SD033 (там было 0,35), а речь стоя даёт более высокую корреляцию. Материал для маски по когерентности есть; превратится ли он в распознанные команды, покажет T4.
  - Наблюдение живой ноды во время записи (одноканальная цепочка SD033, dry run): `d4_still` шесть команд из шести, `d2_drive_noise` три из шести, `d1_drive` ни одной, `d3_drive_talk` ложных нет. Это отправная точка для сравнения в T4, а не результат бенча.
- **T3 закрыт 2026-09-16** (всё в сборщике): ступень встроена в `CommandPipeline` перед сведением в моно (буфер интерливед-звука, целые кадры 10 мс, `dual_ms` в таймингах, сброс вместе с конвейером, `dual_name()` и `dual_error()`), бенч получил `--dual` и параметры методов.
  - AC1: старые проверки SD033 прошли без изменений, в том числе совпадение блока с `Decimator(3)` от `mix_to_mono` при `dual: none`;
  - AC2: при двух каналах и `dual: coherence` блок для распознавателя отличается от простой полусуммы, `dual_ms > 0`;
  - AC3: при одном канале и `dual: nlms` конвейер работает, `dual_name()` равен `none`, ошибка непуста;
  - AC4: дымовой прогон бенча на записях SD035 печатает обе ветки, имена конфигураций содержат `;d=none` и `;d=coherence;n=512;fl=-18`;
  - сборка без предупреждений, `colcon test` 11 из 11, `pre-commit` чистый после переформатирования clang-format;
  - первое наблюдение на реальном материале: с `denoise: none` обе ветки дают 0 из 6 на `d1_drive` и `d2_drive_noise`; маска добавляет около 6 % ко времени обработки (под эмуляцией 1913 против 2037 `ms_per_s`). Полное сравнение — T4.
- **T4, сравнение методов 2026-09-16 (сборщик, 6 вариантов × 4 сессии, 3 минуты): двухканальная ступень не дала выигрыша.** Таблица в As-built `tech.md`.
  - `d1_drive` (едет, 3–4 м, обычный голос): 0 из 6 во всех шести вариантах;
  - `d2_drive_noise`: 3 из 6 только у одноканального `speexdsp` без ступени, у всех остальных 0 из 6;
  - `d4_still` (контроль стоя): 6 из 6 везде, кроме `nlms` — он обнулил и её;
  - `d3_drive_talk`: лишних команд нет ни у кого;
  - итого: `speexdsp; d=none` 9 из 18, `d=none` и `d=coherence` по 6 из 18, `nlms` 0 из 18;
  - `ms_per_s` под эмуляцией: ступень добавляет 8 % (`coherence`) и 11 % (`nlms`).
  - Почему `nlms` провалился: микрофоны стоят рядом, речь попадает и в опорный канал, поэтому адаптивный фильтр вычитает её вместе с шумом. Это было записано как риск в D2.1.
  - Почему `coherence` не помог: шум между каналами почти не коррелирован, но и речь на 3–4 м на ходу даёт слишком слабую когерентность, чтобы маска её выделила. Ближний случай ступень не испортила (стоя 6 из 6).
  - **Значение `dual` по умолчанию не выбрано, T4 не закрыт.** Перебор параметров (`nlms_mu`, `coherence_fft`, порог) не запускался: при 0 из 6 у обеих веток на всех ходовых сессиях крутить настройки нечего. Решение вынесено оператору, как в SD033.
- СА утверждён 2026-09-16 в UI режима Plan. Канон: [`tech.md`](tech.md), 20 пунктов, задачи T1–T6. Решения, которых не было в вопросах: ступень встаёт в `CommandPipeline` перед сведением в моно (одна точка для ноды и бенча); методы `nlms` (второй канал как опора шума) и `coherence` (маска по когерентности, свой радикс-2 FFT, задержка около 11 мс); новых внешних библиотек нет, образ сборщика и runtime-образ не меняются; в записях новой серии по шесть команд на сессию (прогон приёмки остаётся 3–6 команд, как просил оператор).
