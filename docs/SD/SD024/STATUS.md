# STATUS: Линейный канал следования (подъезд и отъезд)

sd: SD024

phase: closed
ba: approved
design: skipped
sa: approved

## Скипы
- BA: —
- Design: экраны и визуальные компоненты не меняются — закон движения и параметры ноды, UI нет (решение оператора).

## Заметки
- **SD024 ЗАКРЫТ (2026-09-06, решение оператора).** Робот следует за человеком по дистанции:
  подъезд, отъезд, покой в коридоре, без доворота. Приёмка качественная — вердикт оператора на
  стенде, как в SD022. Задачи T1–T6 закрыты.
- Хвост в следующий эпик: скорость следования ниже желаемой. Сейчас `max_linear_follow` 0.20 м/с
  при путевом потолке платформы 0.37, `kp_lin` 0.8, `min_breakaway_linear` 0.10. Разгонять вместе
  со снятием обоих временных выключателей (линейный SD022, угловой SD024) и сведением каналов в
  один закон.
- `solution.md` утверждён оператором (2026-09-06), дизайн скипнут его же решением; SA закрыт, фаза — реализация.
- Заведён из закрытия [SD022](../SD022/STATUS.md) по решению оператора (2026-09-06): SD022
  проверил угловой канал в изоляции, SD024 так же проверяет линейный, а сведение обоих в один
  закон — следующий эпик, отдельным SD.
- Ответы BA (оператор): по дистанции — **коридор** (подъезд и отъезд, внутри стоять); поворот
  на время SD — **угловая принудительно в ноль**, зеркально патчу SD022; риск без F12/F13
  ограничиваем **потолком скорости на время SD** и **жёстким стоп-порогом по дальности**;
  приёмка — **качественная, как в SD022**.
- Инвариант «нет человека — не едем» в коде **не выполнялся**: лок коустил последнюю позу до
  `target_lost_s = 2 с` при `valid = true` (`person_target_lock.hpp`). Решение оператора — закрыть
  разрыв признаком `coasting` в msg, а не правкой BA.
- Патч SD022 (`D7`, `kSd022MinBreakawayAngular`, `linear_x = 0`) **снят** и заменён временным
  выключателем углового канала; **оба** временных выключателя (SD022 и SD024) снимаются в
  следующем эпике.
- Числа коридора и стоп-порога в BA не заданы; T5 не свипили стиком/IMU (на стоящем роботе
  IMU врёт «едет»). Оператор 2026-09-06: следование уже работает, только чуть быстрее —
  `max_linear_follow` 0.15 → 0.20. Остальные I3 без изменений.
- Хвост стенда, унаследованный из SD022: глубина `/aurora/points2` идёт 2.15 Гц вместо
  штатных 10 даже на штатной батарее. На линейный канал влияет сильнее, чем на угловой —
  дистанция обновляется реже.
- Код написан и прошёл код-ревью оператора (2026-09-06). Закрыты T1 (закон + тесты), T2 (признак
  `coasting`: msg 1.2.0, лок, перцепшн 0.5.0, `points_timeout_ms` 700), T3 (параметры, валидация
  соотношений, гейт `!coasting`, `motion_control` 0.3.0), T4 (launch, bringup 0.4.0), T6 (README,
  STATUS). **T5 закрыт решением оператора** (2026-09-06): свип IMU отменён; в launch
  `max_linear_follow` 0.20, коридор и `stop_range` — I3; As-built дописан.
- Дальше на пользователе: сборка, деплой, QA.
