# STATUS: Голосовые команды на ходу

sd: SD033
phase: closed
ba: approved
design: skipped
sa: approved

## Скипы
- BA: —
- Design: экраны и визуальные компоненты не меняются

## Заметки
- **SD033 ЗАКРЫТ (2026-09-16, решение оператора: «проезд сделал, работает в среднем плохо даже с 3-4 метров плохо слышит, давай закрывать SD»).** На ходу распознавание осталось слабым даже с 3–4 м. Задачи T1–T6 отмечены закрытыми, канон: [`tech.md`](tech.md). Не коммичено, коммит и релиз за оператором.
- Что получилось: стоя команды работают как в SD031 (на записях 6 из 6); команда принимается по промежуточному результату на 0,7–1,0 с раньше финального; по умолчанию включена очистка SpeexDSP; у ноды появились запись звука, статистика уровня и времени обработки, управление усилением микрофона; есть офлайн-бенч `voice_bench`, разметка и фикстуры вне git. Голосовой конвейер на Pi стоит около 7 % одного ядра (64,5 мс/с без очистки, 69,5 со SpeexDSP).
- Что не получилось: на ходу с 3–4 м обычным голосом команды не проходят (на записях 0 из 6), в проезде оператора «в среднем плохо». Причина измерена: отношение сигнал/шум в полосе речи от −7 до −1 дБ. Дальность может вытянуть только другой способ подавления шума или железо, для этого заведена заготовка [`../SD035/STATUS.md`](../SD035/STATUS.md).
- Версии на закрытии: `mentorpi_voice` 0.2.0 (было 0.1.1), остальные пакеты не менялись. Образ сборщика пересобран (RNNoise `372f7b4`, SpeexDSP 1.2.1). Runtime-образ `mentorpi-t1` и `mk/provision.sh` не менялись, provision не нужен.
- Приёмка T6 неполная: оператор прогнал проезд и вынес вердикт, числами не зафиксированы доля прошедших команд на ходу и стоя (AC1–AC3), поведение пульта и `t1ctl` (AC5), загрузка ядер и частота рамок детекции рядом с 64,7–67,1 % SD031 (AC6). AC4 (работа при отказе очистки) закрыт на стенде в T5, AC7 (границы диффа) проверен агентом.
- Хвосты: в установленном YAML на стенде может остаться `capture_gain_percent: 50`, вернуть `scratch/sd033/t1_stand.sh gain -1`; обход Bluetooth-колонки в контейнере отсутствует, ответ уходит в молчащий WonderEcho; временный скрипт `scratch/sd033/t1_stand.sh`, записи и фикстуры в `scratch/sd033/` остаются на машине разработки и пригодятся для SD035.
- Заведён 2026-09-14 после SD031 (F21). Наблюдение оператора: пока крутятся гусеницы, шасси шумит, и голосовые команды не проходят до остановки. Запрос: добавить в пайплайн очистку звука или выделение человеческого голоса, оператор предполагает ещё одну нейросеть.
- Что известно из SD031 на момент заведения:
  - `voice_command` подаёт захват ALSA (16 кГц, моно) прямо в Vosk 0.3.45 с грамматикой из трёх фраз, предобработки звука нет;
  - команда берётся только из финального результата Vosk, а фраза закрывается паузой после речи;
  - усиление микрофона `Mic Capture Volume` 412 из 496 (25,75 дБ);
  - запись с микрофона на ходу не снималась, поэтому причина не подтверждена: шум не даёт распознавателю закрыть фразу или заглушает саму речь.
- Нагрузка: детекция offline занимает ядра Pi 5 на 63–69 % (SD030), с голосом 64,7–67,1 % (SD031 T7 AC6).
- Стенд после SD031 во временной конфигурации: ответ идёт в Bluetooth-колонку (`playback_device: "bt"`, `mute_tail_ms: 1000`), подробности в STATUS SD031.
- Тексты SD проходят через скилл humanizer-ru (просьба оператора).
- Ответы оператора на вопросы BA (2026-09-14):
  - на ходу нужны все три команды;
  - команду даёт любой человек, примерно в 3–4 м от робота;
  - учитывать любой шум;
  - пропущенная команда хуже ложной;
  - железо менять нельзя;
  - бюджет CPU и доля распознанных команд: «сколько получится в этой итерации, не принципиально».
- BA черновик 2026-09-14: [`solution.md`](solution.md). Два места записаны без прямого ответа оператора, проверить на гейте:
  - ответ на вопрос 6 прочитан так: допустимая загрузка CPU, просадка частоты детекции и целевая доля распознанных команд не заданы;
  - при отказе очистки звука голос работает как в SD031, на стоящем роботе (негативный №3).
- BA утверждён 2026-09-14 («ок»), оба места выше оператор не поправил. Этап сменён на системный анализ, но СА оператор попросил пока не начинать.
- 2026-09-15 оператор попросил прочитать контекст и задать вопросы СА, сам СА не начинать. Что видно в коде на момент вопросов:
  - `conf/model.conf` модели `vosk-model-small-ru-0.22` закрывает фразу только по тишине после речи (`endpoint.rule2–4`: 0,5 / 1 / 2 с). Если шум распознаётся не как тишина, фраза закрывается только по длине, в Kaldi по умолчанию через 20 с. Это похоже на наблюдение оператора, но записи с хода нет;
  - `voice_command` берёт только финальный результат Vosk, промежуточный не смотрит;
  - WAV-фикстур `test_vosk_phrases` (`src/mentorpi_voice/test/data/*.wav`) нет ни в git, ни на диске, тест сейчас упадёт;
  - в `ChassisStatus` признака движения нет, судить о нём можно по `/vehicle/cmd_vel` или `/odom_raw`.
- Ответы оператора на вопросы СА (2026-09-15):
  1. Запись звука на ходу перед выбором метода: (а). Первая задача SD033 добавляет в `voice_command` режим записи (WAV до и после очистки), оператор ведёт робота, команды говорят с 3–4 м, агент разбирает записи. Записи становятся фикстурами теста «на ходу» вместо пропавших.
  2. Менять можно и распознаватель, не только чистить звук: принимать команду по целой фразе в промежуточном результате, закрывать фразу по длине.
  3. Метод очистки: (в), сравнить лёгкую нейросеть и классическую обработку на записях без робота, взять лучший по доле распознанных команд при приемлемой загрузке CPU. Очистка в потоке захвата `voice_command`, при отказе звук идёт в распознаватель без обработки.
  4. Один профиль всегда (а): очистка включена и стоя, и на ходу.
  5. Усиление микрофона: «наверное (а), мне сложно выбрать». Прочитано как параметр усиления захвата в `voice_command`; нужно ли его менять и какое значение ставить, решают записи из п. 1.
  6. Ответ в Bluetooth-колонку остаётся вне репозитория (а): после каждой выкладки оператор возвращает обход руками, шаг попадёт в финальный QA.
  7. WAV с голосами в git класть нельзя. Записи с хода и фикстуры тестов живут вне репозитория.
  8. Сборка за агентом (`make env-overlay`, `make build`, `colcon test` в сборщике, `pre-commit`), выкладка (`make deploy`, `make provision`) и стенд за оператором.
- BA дополнен 2026-09-15 по ответу 2: в «Решении» и функциональных требованиях команда принимается по целой фразе без ожидания паузы, в том числе на стоящем роботе. Правка утверждена («ок»).
- СА утверждён 2026-09-15: план собран в режиме Plan, выход из Plan оператор отклонил и сказал «пиши tech.md, но к выполнению не приступай». Канон: [`tech.md`](tech.md), 39 пунктов, задачи T1–T6.
- Решения СА, которых не было в вопросах: очистки для сравнения RNNoise 0.2 и SpeexDSP 1.2.1, обе статически в образе сборщика (runtime-образ и provision не меняются); закрытие фразы по длине сделано параметром `max_utterance_ms` в коде, а не правкой `conf/model.conf`; для записи на ходу добавлен `publish_commands: false`, чтобы режим не менялся посреди езды; каталог записей на стенде `/home/pi/docker/tmp/voice_rec` (в контейнере `/home/ubuntu/shared/voice_rec`); на хосте разработки записи и фикстуры в `scratch/sd033/`.
- 2026-09-15 около 19:25 `STATUS.md` и `solution.md` вернулись к версии из коммита (`tech.md` остался в индексе). По решению оператора правки BA и заметки внесены заново, `tech.md` не менялся.
- Реализацию оператор запустил 2026-09-15 («ок, начинай»), начата T1.
- T1 код 2026-09-15 (агент), стендовая часть за оператором. Что сделано: `audio_dsp.hpp`, `wav_writer.hpp`; в `voice_command` захват на `capture_rate`, FIR-прореживание до 16 кГц, запись отдельным потоком, `publish_commands`, строка `audio stats`; `mentorpi_voice` 0.2.0. Проверено в сборщике:
  - сборка без ошибок, `colcon test` 5 из 5 (`test_vosk_phrases` исключён: фикстур нет, это T4), `pre-commit` зелёный;
  - AC1 и AC2 закрыты (`test_audio_dsp`, `test_wav_writer`);
  - AC3 закрыт: `capture_rate:=44100` роняет ноду с текстом `capture_rate must be a multiple of sample_rate (got capture_rate=44100 sample_rate=16000)`, при `16000` нода жива и пишет `capture open failed`;
  - на ALSA-устройстве `null` с `record_max_mb:=1`: `record open`, пара `_raw.wav` (48 кГц моно, 8,2 с) и `_asr.wav` (16 кГц моно, 8,1 с) читается, затем WARN `record stopped: limit`, `audio stats` идут дальше. Это часть AC4 без стенда.
  - `make build` зелёный (16 пакетов и `t1ctl`), сборка готова к выкладке;
  - Ждут стенда: AC4–AC7.
- Имена файлов записи берут местное время контейнера; в сборщике это UTC.
- 2026-09-15 на стенде в контейнере `mentorpi-t1` нет `arecord` («command not found»); As-built SD031 считал `alsa-utils` частью runtime-образа, сейчас это не так. Частоты и каналы захвата для T1 AC7 берутся из `/proc/asound/Device/stream0`, в `tech.md` T1 проверка поправлена.
- По просьбе оператора сделан временный скрипт `scratch/sd033/t1_stand.sh` (вне git): `deploy`, `default`, `limit`, `record <сессия>`, `log`, `hw`, `fetch`. `record` одной командой ставит dry run и запись в каталог сессии, перезапускает стек, показывает журнал до Ctrl+C, возвращает обычный YAML с обходом колонки и копирует WAV в `scratch/sd033/rec/<сессия>/` с проверкой пика и отсечения (заголовок WAV чинится, если стек убил ноду до закрытия файла). Удалить после T1.
- T1 стенд 2026-09-15 (оператор): выкладка скриптом, записаны четыре сессии; пары `_raw.wav` 48 кГц и `_asr.wav` 16 кГц моно лежат в `scratch/sd033/rec/`:
  - `s1_still` 45,1 с, `s2_drive` 29,4 с, `s3_drive_noise` 38,5 с, `s4_drive_talk` 41,4 с;
  - пик от −15,5 до −20,3 dBFS, отсечения нет ни в одной сессии: усиление `Mic` 412 из 496 менять не нужно;
  - команд оператор говорил меньше 15 на сессию («изнурительно и излишне»), D4.3 в `tech.md` поправлен: число по факту, последовательность в `labels.tsv`;
  - каталог `limit` в момент осмотра был пуст, потому что прогон `limit` ещё шёл (итог во втором прогоне ниже). Отдельный `fetch` оператор не запускал: `limit` и `record` копируют файлы сами.
- T1 стенд, второй прогон 2026-09-15 (оператор, журнал из вывода скрипта). Сессии перезаписаны: `s1_still` 31,5 с, `s2_drive` 28,9 с (пик −5,8 dBFS), `s3_drive_noise` 24,5 с, `s4_drive_talk` 22,0 с, `limit` 8,2 с; отсечения нет.
  - AC4 закрыт на стенде: `record open`, через 8 с WARN `record stopped: limit`, `audio stats` идут дальше.
  - AC6 закрыт: `audio stats` раз в 10 с (тихо стоя RMS −42…−47 dBFS, на ходу около −33 dBFS).
  - AC5, часть dry run, закрыта: в `s1_still` шесть строк `heard … (dry run)` (запрет, следование, ручной, два круга) и одна `ignored "запрет" reason=no match`, режим голосом не менялся. Публикация стоя (`default`) не проверена.
  - `s3_drive_noise`: за 24 с езды с шумом ни одной строки `heard` или `ignored`. Похоже на гипотезу из СА: в шуме финальный результат не приходит. Проверим бенчем в T4.
  - Журнал `s2_drive` в терминале оператора обрезан, распознанного не видно.
  - Оператор: одиночное «запрет» в `s1_still` было оговоркой (забыл «режим»), в сессиях второго прогона команды шли в порядке протокола (запрет, следование, ручной по кругу).
  - AC5 закрыт полностью: `default` стоя, четыре команды (запрет, ручной, запрет, следование) дали `heard` без `(dry run)` и `announce … trigger=voice` на каждую; ответ политика пишет только после прихода `/control/state` в нужный режим.
  - В YAML стенда остался `playback_device: "bt"` от первого запуска скрипта, при этом `pcm.bt` в контейнере нет: `playback failed device=bt` на кнопку пульта. Скрипт поправлен: без `pcm.bt` он ставит WonderEcho и `mute_tail_ms: 300`, а при прерывании `limit` или `record` возвращает обычный YAML.
- Число кругов в сессиях на ходу оператор уточнил 2026-09-15: `s1_still` два круга (шесть команд, оговорка «запрет» не команда), `s2_drive` и `s3_drive_noise` по одному кругу (три команды), `s4_drive_talk` без команд. Счёт речевых всплесков по энергии (фильтр ~300 Гц, кадры 50 мс) сошёлся с журналом только в `s1_still`; в сессиях на ходу голос тонет в шуме (фон около −35 dBFS), всплесков не найдено. Разметка в `scratch/sd033/rec/labels.tsv` (вне git).
- **T1 закрыт 2026-09-15:** AC1–AC3 в сборщике, AC4–AC6 на стенде оператором, AC7: четыре сессии и `labels.tsv` в `scratch/sd033/rec/`, As-built захвата в `tech.md`, WAV в git нет. Дальше T2.
- T2 начат 2026-09-15 («давай t2»): блок RNNoise и SpeexDSP в `docker/overlay-builder/Dockerfile`, `denoiser.hpp`, `test_denoiser`, CMake I14.2. Первый `make env-overlay` (прокси `HTTP_PROXY=http://172.17.0.1:8888`, слои ncnn и Vosk из кеша, модель RNNoise `rnnoise_data-0b50c45.tar.gz` скачалась) упал на RNNoise `v0.2`: `src/vec_neon.h:35:10: fatal error: os_support.h: No such file or directory`. Upstream исправил это коммитом `372f7b4` «Fix compilation errors.» (16.04.2024, через день после `v0.2`; правит только инклюды и `OPUS_CLEAR` → `RNN_CLEAR`, `model_version` не меняется). RNNoise закреплён на этом коммите, D2.1, I14.1 и T2 в `tech.md` поправлены.
- **T2 закрыт 2026-09-15** (всё в сборщике, стенд не нужен):
  - AC1: второй `make env-overlay` зелёный (слои ncnn, Vosk и apt из кеша); в образе `/opt/rnnoise/lib/librnnoise.a` (1,75 МБ, модель внутри), `rnnoise.h`, `/opt/speexdsp/lib/libspeexdsp.a`, `speex/speex_preprocess.h`, заданы `RNNOISE_ROOT` и `SPEEXDSP_ROOT`;
  - AC2, AC3: `colcon test` `mentorpi_voice` 6 из 6 (`test_vosk_phrases` исключён до T4); `test_denoiser` на белом шуме −20 dBFS после первой секунды: `rnnoise` −81,2 дБ, `speexdsp` −24,6 дБ; отказ фабрики (`rnnoise` на 16000, `bogus`, `speexdsp` с +5 дБ) даёт `none` и текст ошибки;
  - AC4: `make overlay` зелёный (16 пакетов), `docker/mentorpi-t1/Dockerfile` и `mk/provision.sh` в диффе нет; `pre-commit` зелёный;
  - SpeexDSP печатает `warning: The VAD has been replaced by a hack pending a complete rewrite` на каждый `SPEEX_PREPROCESS_SET_VAD`, даже с выключением. VAD и так выключен по умолчанию, вызов убран, предупреждения больше нет;
  - что даст очистка на речи с хода, покажет бенч T4. Цифры на белом шуме только говорят, что обе библиотеки работают.
- **T3 закрыт 2026-09-16** (всё в сборщике): `phrase_match.hpp` (`split_words`, `find_phrase`, `match_phrase` удалён), `utterance.hpp` (слова из результата, `parse_vosk_partial`, уверенность по словам фразы, `PartialTrigger`), `command_pipeline.hpp` (`AsrEngine`, `VoskEngine`, `CommandPipeline`), тесты `test_phrase_match` (переписан), `test_utterance`, `test_command_pipeline` на подставном распознавателе.
  - сборка без предупреждений (`-Wall -Wextra -Wpedantic`), `colcon test` 8 из 8 (`test_vosk_phrases` исключён до T4), `pre-commit` зелёный после переформатирования clang-format;
  - AC1–AC7 закрыты тестами: промежуточная фраза, державшаяся 200 мс, даёт одну команду на 300 мс и сброс; принудительный финал приходит ровно на 5000 мс при блоках 100 и 10 мс, при `0` не вызывается; при `partial_trigger: false` промежуточный результат не читается; блок для Vosk побитно равен `Decimator(3)` от моно; неизвестный `denoise` даёт `none` и ошибку;
  - нода SD031 уже вызывает `decide_command`, поэтому на её финальном результате с T3 действуют поиск фразы подряд и уверенность только по словам фразы. Промежуточный результат, очистку и предел длины нода получает в T5; до выкладки T5 на стенде стоит сборка T1.
- T4 начат 2026-09-16 («ок»): `bench_score.hpp`, `voice_bench`, `test_bench_score`, `test_vosk_phrases` через `CommandPipeline` и `MENTORPI_VOICE_TEST_DATA_DIR`. Сборка без предупреждений, `colcon test` 10 тестов, `test_vosk_phrases` без переменной пропущен (код 77); `voice_bench` с несуществующим файлом в разметке даёт код 2 и имя файла; RUNPATH `$ORIGIN/..`.
- T4 бенч, первый прогон 2026-09-16 (сборщик, `--jobs 12`, около 3 мин): шесть вариантов (`none`, `rnnoise`, `speexdsp` × промежуточный результат 0/1; `partial_stable_ms` 200, `max_utterance_ms` 5000, `min_confidence` 0.6):
  - `s1_still`: 6 из 6 во всех вариантах; с промежуточным результатом команда приходит на 0,7–1,0 с раньше финального (например, 7,3 с против 8,1 с);
  - `s2_drive` и `s3_drive_noise`: 0 из 3 в каждой сессии во всех вариантах; ни очистка, ни промежуточный результат, ни предел длины фразы не дали ни одной команды;
  - `s4_drive_talk`: лишних команд нет;
  - с очисткой оговорка «запрет» в `s1_still` сливается со следующей командой («запрет режим запрет»), поиск фразы подряд её находит;
  - `ms_per_s` под эмуляцией 2740–3170, для Pi не показательно.
  Критерии D2.4 по такому результату значение не выбирают: на ходу все варианты равны нулю. Следующий шаг: трассировка того, что распознаватель выдаёт на ходу, с грамматикой и без неё (`--trace`, `--free`).
- T4 трассировка сессий на ходу 2026-09-16 (`s2_drive`, `s3_drive_noise`, `s4_drive_talk`, `--trace`, `--free`):
  - с грамматикой Vosk ни разу не закрыл фразу сам: 14 финалов на каждый вариант, все принудительные по `max_utterance_ms`, текст почти всегда пустой. Промежуточные результаты без очистки и со SpeexDSP почти пустые (одно «запрет» в `s4_drive_talk`, где команд не было); с RNNoise отдельные слова «режим», «ручной», «следование», целой фразы нет, и слова не совпадают с разметкой (в `s3_drive_noise` «ручной» трижды при одном сказанном);
  - без грамматики (полный словарь модели) без очистки пусто, с RNNoise «сваты», «просто мало», «фильм», «в грудь и меня было», «рот», «вруби на», «видите»; слов команд нет;
  - отношение сигнал/шум по кадрам 50 мс: речь стоя с 3–4 м (p95 `s1_still`) −37,4 dBFS, в полосе 300–3400 Гц −40,3 dBFS; медиана шума на ходу `s2_drive` −28,1 (в полосе −33,5), `s3_drive_noise` −33,1 (−38,9), `s4_drive_talk` −33,3 (−39,2). Отношение от −9,3 до −4,1 дБ по полной полосе и от −6,8 до −1,1 дБ в полосе речи;
  - вывод: гипотеза СА о закрытии фразы по тишине не подтвердилась. На ходу голос с 3–4 м тише шума гусениц уже на микрофоне WonderEcho, и ни очистка, ни настройки распознавателя в этих записях не дали ни одной команды. Это расходится с функциональным требованием BA (команды на ходу с 3–4 м) при запрете менять железо. **T4 остановлен до решения оператора.** Код T1–T3 и бенч от решения не зависят.
- Решение оператора по остановке T4 (2026-09-16, «давай 1 и 2»): до правки BA проверить два пути без смены железа. (1) Различаются ли два канала захвата WonderEcho (если это два микрофона, шум гусениц можно гасить по разнице каналов). (2) Граница распознавания на ходу по расстоянию и громкости. Записи `s5_drive_2ch`, `s6_drive_1m`, `s7_drive_loud`: все в два канала, один круг команд; D4.3 в `tech.md` дополнен, скрипт стенда умеет `capture_channels`. Варианты «команда по одному слову» и «другой микрофон» отложены.
- Диагностические сессии 2026-09-16 (оператор, все в два канала, один круг команд): `s5_drive_2ch` 21,4 с, `s6_drive_1m` 18,7 с, `s7_drive_loud` 18,8 с, отсечения нет. В `s7_drive_loud` живая нода на ходу распознала «режим ручной» (dry run).
  - Каналы WonderEcho различаются: одинаковых отсчётов 0,1–0,2 %, разность L−R всего на 2–4 дБ ниже самого канала, лучшая взаимная корреляция 0,35 при сдвиге до 0,5 мс. Это два разных микрофона, шум между ними почти не коррелирован. Простое усреднение каналов (то, что делает конвейер сейчас) даёт не больше 3 дБ.
  - Бенч по трём сессиям (6 вариантов) и перебор (`partial_stable_ms` 100/200, `min_confidence` 0.30/0.60, `speex_db` −20/−30/−40, 20 вариантов): `s5_drive_2ch` (3–4 м, обычный голос) 0 из 3 везде; `s6_drive_1m` (1 м, обычный голос) `none` 0 из 3, `rnnoise` 1 из 3, `speexdsp` 2 из 3; `s7_drive_loud` (3–4 м, громко) `none` и `speexdsp` 2 из 3, `rnnoise` 1 из 3. Лишних команд нет нигде. Значения `partial_stable_ms`, `min_confidence` и `speex_db` на результат не влияют.
  - В `s6_drive_1m` и `s7_drive_loud` пропущена первая команда круга (`mode_forbid`): возможно, её произнесли сразу после `record open`.
  - Вывод: с этим микрофоном на ходу команда проходит примерно с 1 м обычным голосом (со SpeexDSP) или с 3–4 м громко. При 3–4 м обычным голосом не работает ничто. SpeexDSP полезен на границе (0 → 2 из 3 на 1 м), RNNoise хуже (пережимает речь). Требование BA «на ходу с 3–4 м, любой человек» этой сборкой не выполняется: решение оператора о правке BA или о новом пути (двухканальное подавление) ожидается.
- Решение оператора 2026-09-16 («давай в этом sd вариант 1, но заведи SD под вариант 2»): SD033 доводится под измеренные границы, двухканальное подавление вынесено в отдельный SD.
  - BA дополнен (`ba: drafted`, этап снова бизнес-анализ, ждёт утверждения): в «Решении» и сценариях на ходу команда проходит примерно с 1 м обычным голосом или с 3–4 м громко, стоя как в SD031; ФТ про дальность переписано; в НФТ добавлены измеренные −7…−1 дБ и исключено подавление по двум микрофонам.
  - Заведена заготовка [`../SD035/STATUS.md`](../SD035/STATUS.md) (подавление шума по двум микрофонам): только факты и замеры, требований нет, вопросы BA не задавались.
  - После утверждения BA в T4 остаётся: записать в As-built выбранные значения (`denoise: speexdsp`, промежуточный результат включён, остальное стартовое), вырезать фикстуры для `test_vosk_phrases`, получить от оператора один прогон бенча на Pi для `ms_per_s`.
- Правка BA утверждена 2026-09-16 («ок»), этап снова реализация.
- T4 закрыт кроме AC5 (2026-09-16):
  - AC1 `test_bench_score`, AC3 негативная проверка бенча, AC6 (в git нет WAV и разметки) закрыты; AC2 закрыт полностью: без переменной тест пропускается (код 77), с фикстурами `test_vosk_phrases: ok`;
  - фикстуры вырезаны из записей и лежат в `scratch/sd033/fixtures/` вне git: `mode_forbid.wav` (`s1_still` 6,1–8,0 с), `mode_follow.wav` (9,1–11,8), `mode_manual.wav` (13,6–15,9), `other.wav` (`s4_drive_talk` 5,0–8,0);
  - AC4 закрыт: таблица бенча по шести сессиям и выбранные значения записаны в As-built `tech.md` (`speexdsp`, промежуточный результат включён, `partial_stable_ms` 200, `max_utterance_ms` 5000, `min_confidence` 0.6, `speex_noise_suppress_db` −30, `capture_gain_percent` −1);
  - AC5 (замер `ms_per_s` на Pi) открыт: на стенде сейчас сборка T1, где `voice_bench` ещё нет. Разумно снять замер после выкладки T5, за один выезд с приёмкой.
- T5 начат 2026-09-16 («да»): в `voice_command` поток захвата кормит `CommandPipeline` (очистка, прореживание, промежуточный результат, предел длины фразы), события конвейера превращаются в публикацию, dry run и логи с `via=`; на заглушке ответа конвейер сбрасывается целиком; добавлено управление усилением через микшер ALSA (`capture_gain_percent`, проверка на каждой строке статистики); строка `audio stats` получила `denoise_ms_per_s`, `asr_ms_per_s` и `gain`. В YAML записаны значения из As-built T4, нода линкуется с очисткой.
- T5 код 2026-09-16 (агент), стендовая часть за оператором:
  - `pre-commit` зелёный, сборка без предупреждений, `colcon test` 10 из 10 с фикстурами (AC6), `make build` зелёный;
  - AC1 закрыт: `ldd` установленной ноды без `not found`, RNNoise и SpeexDSP статические (среди разделяемых их нет), `libvosk.so` берётся из `lib/` пакета; в `install/mentorpi_voice/lib/mentorpi_voice/` лежит и `voice_bench`, так что после выкладки снимается T4 AC5;
  - часть AC2 закрыта локально: строка `voice_command pipeline (...)` с параметрами I3 и I4, `denoise ready kind=speexdsp rate=48000`;
  - часть AC3 закрыта локально: `denoise:=bogus` даёт ERROR `denoise init failed kind=bogus: unknown denoise kind 'bogus' (none|rnnoise|speexdsp), using none`; `speex_noise_suppress_db: 5.0` и `capture_gain_percent: 200` роняют ноду на старте с именем параметра;
  - ждут стенда: `heard`/`ignored` с `via=` и `audio stats` с таймингами (AC2), команды стоя при отказе очистки (AC3), микшер (AC4), заглушка микрофона во время ответа (AC5).
- Стенд T5 и T4 AC5 2026-09-16 (оператор, сборка T5 выложена):
  - **T4 закрыт.** AC5: бенч на Pi (`--jobs 1`, контур поднят) дал `ms_per_s` 64,5 (`none`) и 69,5 (`speexdsp`), то есть около 7 % одного ядра; очистка добавляет примерно 5 мс/с. Цифры в As-built `tech.md`. Счёт команд на Pi тот же, что в сборщике: стоя 6 из 6, на ходу 0 из 6 на старых записях.
  - T5 AC2 закрыт: `denoise ready kind=speexdsp rate=48000`, `heard "…" conf_min=-1.00 via=partial -> mode_…`, `ignored "…" conf_min=1.00 via=final reason=no match`, `audio stats … denoise_ms_per_s=5,9–7,7 asr_ms_per_s=38,7–72,5 gain=…`.
  - T5 AC3 закрыт: с `denoise: bogus` ERROR `denoise init failed … using none`, три команды стоя прошли.
  - T5 AC5 подтверждён по журналу: в окне ответа строк `heard` и `ignored` от своей фразы нет, следующая команда принимается. Звука по-прежнему нет: обход колонки из контейнера пропал, ответ идёт в молчащий WonderEcho.
  - T5 AC4 закрыт наполовину: `mixer gain set control=Mic percent=50` и `gain=50%` в статистике есть, проверка дрейфа не прошла. Ошибка в моей команде: `amixer` есть на хосте Pi, а не в контейнере (как и `arecord`). Проверяем с хоста: `amixer -c Device sset Mic 80%`.
- **T5 закрыт 2026-09-16.** AC4 добит на стенде: после ручного `amixer -c Device sset Mic 80%` на хосте Pi нода за 10 с написала `mixer gain drift control=Mic now=80% target=50%, reapply` и вернула `gain=50%`. Остальные критерии закрыты раньше (сборка и `ldd`, стартовые строки и `denoise ready`, отказ очистки, `via=` в логах, статистика с таймингами, заглушка ответа, `colcon test` 10 из 10).
- Хвост стенда: в установленном YAML осталось `capture_gain_percent: 50`, в репозитории `-1`. Перед приёмкой T6 вернуть командой `scratch/sd033/t1_stand.sh gain -1`.
- Границы диффа перед T6 (2026-09-16): изменены только `docker/overlay-builder/Dockerfile`, `src/mentorpi_voice/**` и `docs/SD/SD033/**`; из нового в дереве ещё `docs/SD/SD035/STATUS.md` (заготовка по решению оператора), поэтому в T6 AC7 он добавлен как допустимый. Из корня репозитория удалены шесть дампов `qemu_*.core`: их оставили мои прогоны ноды в сборщике, где `timeout` убивал процесс под эмуляцией.
- As-built захвата T1 записан в `tech.md`: WonderEcho отдаёт только 48000 Гц, 2 канала; обхода колонки в контейнере нет (нет `pcm.bt` и `libasound2-plugins`), скрипт больше не ставит `bt`, если `pcm.bt` нет.
