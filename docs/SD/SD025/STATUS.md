# STATUS: Сведение каналов следования и закрытие F11

sd: SD025
phase: closed
ba: approved
design: skipped
sa: approved

## Скипы
- BA: —
- Design: экраны и визуальные компоненты не меняются — закон движения и параметры ноды, UI нет (решение оператора).

## Заметки
- **SD025 ЗАКРЫТ (2026-09-06, решение оператора).** Робот ведёт человека обоими каналами сразу:
  подъезд с разгоном по дистанции, доворот в движении, покой в коридоре, задний ход и доворот на
  месте отсутствуют. Вердикт оператора на стенде: «работает как надо, реагирует корректно, может
  следовать за мной; чуть медленно, но не страшно». Приёмка качественная, как в SD022 и SD024.
  Задачи T1–T6 закрыты. **Функциональность F11 закрыта** — отметка в каталоге
  [SD001](../SD001/tech.md).
- Версии на закрытии: `motion_control` 0.4.0, `mentorpi_bringup` 0.5.0; `mentorpi_msgs` 1.2.0 и
  `mentorpi_perception` 0.5.0 не менялись. Тесты: `test_follow_control: ok`, сборка overlay
  (13 пакетов) зелёная, `pre-commit` зелёный. Не коммичено — коммит и релиз за оператором.
- Итоговые параметры (T5, [measure.md](measure.md)): `max_linear_follow` `0.25`, `kp_lin` `0.8`,
  `accel_linear` `0.30`, `decel_linear` `0.60`. Три последних не подбирались — поведение принято
  на стартовых значениях.
- **Хвосты за пределами SD025:**
  1. `coasting` встаёт 5 раз за 18.5 с при человеке в кадре. **Уточнено по бэгу после закрытия:**
     сами вспышки короткие — 1–3 тика, суммарно 0.31 с (2 % времени следования); дорого обходится
     не вспышка, а сброс рампы после неё (~0.7 с обратного разгона каждый раз). Трек при этом не
     теряется (`track_id = 15`, `confidence` 0.89–0.93) — детекция ни при чём, наблюдение
     отваливается на слое геометрии.
     **Ещё уточнение: хвост «глубина 2.15 Гц» из SD022 в этом прогоне не воспроизводится.**
     Эффективный период обновления позы: медиана 0.118 с (8.5 Гц), p90 0.254 с, max 0.773 с.
     Проблема не в средней частоте, а в хвосте распределения, и `points_timeout_ms = 700` лежит
     ровно на этом максимуме. Причина отбраковки наблюдения нигде не логируется — отличить
     устаревшее облако от несобравшегося кластера сегодня нечем. Отдельного SD не заводили.
  2. Скорость следования `0.25` при путевом потолке `0.37` — оператор счёл приемлемой.
  3. `stop_request` не сбрасывает рампу (D6.5): сегодня всегда `false`, но с приходом F13
     понадобится подписка на `/control/motion_restriction` со сбросом `prev_linear`.
- design-review skipped: UI не менялся.
- `solution.md` утверждён оператором (2026-09-06), дизайн скипнут его же решением; SA закрыт, фаза — реализация.
- План утверждён в UI Plan; `tech.md` записан: 6 пунктов `D` (23 с подпунктами) + 6 `I`, задачи
  T1–T6, покрытие полное. **К реализации не приступали — указание оператора.**
- Три места, где буква ФТ выполняется не полностью, зафиксированы в `tech.md` D3 и D6.5, а не
  правкой BA: ступенька `kp_lin · dist_deadband` на границе коридора вместо нуля; трогание всегда
  с `min_breakaway_linear`; `stop_request` не сбрасывает рампу (сегодня всегда `false`, источник —
  заглушка `stub_graph`; подписка на `/control/motion_restriction` понадобится с приходом F13).
- Развилки SA (оператор, 2026-09-06): `min_breakaway_linear` — только при трогании с места;
  профиль остаётся P-законом, настраивается `kp_lin`; бюджет угловой — от `max_linear_follow`;
  `dist_deadband_near` и `stop_range` удаляются, `dist_deadband_far` → `dist_deadband`.
- Решение SA, не выносившееся на развилку: приоритет доворота **перевёрнут** относительно SD020
  (`D2.2`). Прежний порядок с бюджетом от `max_linear_follow` обнулял доворот на потолке скорости
  — робот на разгоне переставал подруливать.
- **T1–T4 сделаны (2026-09-06).** Закон переписан (профиль, приоритет доворота, страгивание с
  места, рампа), нода переведена на `prev_linear_` со сбросом по гейту, launch и версии
  обновлены. `motion_control` 0.4.0, `mentorpi_bringup` 0.5.0. Сборка overlay в docker (13
  пакетов) зелёная, `test_follow_control: ok`, `pre-commit run --all-files` зелёный.
- Уточнение дизайна, найденное при реализации T1: инвариант D2.6
  (`linear + |ω|·track_half_sum ≤ ceiling`) нарушался **на переходе** — пока рампа догоняет
  свежепониженный `linear_cap`, применённая линейная выше него. Добавлен финальный кламп угловой
  по применённой линейной; в установившемся режиме он ничего не отнимает. `tech.md` D2.5
  дописан.
- Ступенька D3.1 закреплена тестом: на `standoff + dist_deadband` в double точка ложится на волос
  **снаружи** коридора, и закон там честно выдаёт `kp_lin · dist_deadband` (0.04 м/с при нынешних
  числах). Тест проверяет покой на последней точке внутри коридора и саму ступеньку снаружи.
- Дальше: T5 (замер на стенде) — совместно с оператором; T6 (документы) после него.
- **T5 заход 1 (2026-09-06): бэг разобран, найден дефект, замер не закончен.** Бэг
  `scratch/bags/f11_sd025_20260906_164725`. Пульсация скорости объяснена тремя причинами
  (разбор — в `measure.md`): бюджет доворота D2.2 срезал 27 % тиков, `coasting` 4 раза за 15 с,
  и рампа глушила страгивание на 13 % тиков. Последнее — **дефект T1**, исправлен: пол
  `min_breakaway_linear` накладывается и на результат рампы, `tech.md` D2.3 дописан.
- Решения оператора по итогам захода 1: `max_linear_follow` `0.20` → **`0.25`** (половина
  командного envelope пульта `0.5` из SD020); `kp_ang` не трогаем; сброс рампы по `coasting`
  **оставляем как есть** — частота вспышек это вопрос перцепшна, отдельным SD.
- **Хвост для следующего SD:** `/perception/nearest_person` выставляет `coasting` четыре раза за
  15 с при человеке в кадре. На линейный канал это стоит около секунды за вспышку (провал плюс
  обратный разгон). Причина не искалась — вероятно, глубина 2.15 Гц.
- Ошибки инструкции T5, найденные оператором: `t1ctl mode auto` не существует (правильно
  `t1ctl mode allow`, `host/t1ctl/src/main.cpp:224`), а в рецепте ручного перезапуска ноды
  `pkill -f 'motion_control/motion_control'` убивал собственный `bash -lc` — шаблон совпадал с
  его же командной строкой. Перезапуск ноды вручную из протокола убран.
- Заведён из хвоста [SD024](../SD024/STATUS.md): «сведение обоих каналов в один закон — следующий
  эпик, оба временных выключателя снимаются там». Номер SD025 назван оператором.
- Ответы BA (оператор, 2026-09-06):
  1. Задний ход **убирается** — возвращается ФТ SD020 «не пятится». Отъезд и ближняя граница
     коридора из SD024 отменяются.
  2. Доворот на месте **не делаем** — угловая только в движении (ФТ SD020). Временное поведение
     SD022 (доворот на месте) не возвращается.
  3. Разгон — **оба механизма**: профиль скорости от дистанции (далеко → к потолку, у коридора →
     к нулю) **и** ограничение темпа изменения команды (плавный старт/остановка).
  4. Потолок скорости следования — **число в BA не фиксируем**, подбирается замером на стенде и
     пишется в `measure.md`, как в SD022/SD024.
- Из ответа 3 следует инвариант, вписанный в ФТ: ограничение темпа **не** распространяется на
  стоп по гейту (пропажа цели, коуст, выход из `AutoFollow`, запрет) — там ноль мгновенный.
- Состояние кода на входе: `motion_control` 0.3.0, `mentorpi_bringup` 0.4.0, `mentorpi_msgs` 1.2.0,
  `mentorpi_perception` 0.5.0. В `follow_control.hpp` — закон SD024 (линейный коридор с отъездом,
  `angular_z ≡ 0` с пометкой `TEMPORARY, SD024`). Ограничения ускорения нет нигде в overlay.
